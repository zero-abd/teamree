// The branch name previewed while typing a task name. The rule itself is shared
// with the runtime so the preview cannot promise a name that is not created.

export { slugifyBranchName as branchNameFromTask } from '@shared/branchName'
